Flask - The web framework for building the API
Flask-Cors - Allows frontend to make requests from different origin
psycopg2-binary - PostgreSQL database connector for Python
python-dotenv - Reads environment variables from .env file

source venv/bin/activate

pip install -r requirements.txt